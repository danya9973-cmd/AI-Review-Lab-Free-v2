# 변경사항 요약

## Free API Edition

- Claude SDK 및 OpenAI SDK 의존성을 제거했습니다.
- 공급자를 Gemini + Groq + OpenRouter로 교체했습니다.
- 기본 모델을 `gemini-2.5-flash-lite`, `llama-3.1-8b-instant`, `openrouter/free`로 설정하고 모두 환경변수에서 바꿀 수 있게 했습니다.
- 흐름을 독립 분석 3회 + 최종 종합 1회, 총 4회로 단순화했습니다.
- 세 독립 분석은 동시에 실행해 대기 시간을 줄였습니다.
- 웹 UI에 설정 상태, 모델명, 예상 호출 수, 공급자별 실제 연결 테스트, 상세 오류를 추가했습니다.
- CLI 진단 도구에 오프라인 설정 검사와 명시적 `--live` 연결 검사를 추가했습니다.
- API 키를 서버에만 보관하고 응답/오류에 노출하지 않도록 했습니다.
- `127.0.0.1` 바인딩을 강제하고 외부 전송 경고와 보안 응답 헤더를 유지·강화했습니다.
- 실제 네트워크 없이 공급자 파싱, 오류, 키 비노출, 4회 흐름을 검증하는 테스트를 추가했습니다.
